# AudioLens 要件定義書

- 版数: 0.1(ドラフト)
- 作成日: 2026-08-03
- 対象プラットフォーム: Windows 10 (2004 / Build 19041) 以降、Windows 11

---

## 1. プロジェクト概要

### 1.1 目的

AudioLens は、PC で再生されるあらゆる音声を、用途に合わせて「聞き取りやすく」調整するプリセット型オーディオ補助アプリである。専門的な周波数知識を持たない利用者が、プリセットの選択と少数の分かりやすいツマミ操作だけで、以下の効果を得られることを目的とする。

- 低音の濁り(こもり)の抑制
- 人の声に必要な周波数帯の強調による明瞭度向上
- 大きすぎる音・小さすぎる音の音量差の自動平準化

### 1.2 ターゲットユーザー

| ユーザー像 | 代表的な利用シーン |
|---|---|
| オンライン会議・通話の利用者 | 相手の声が聞き取りにくい会議、録音の聞き直し |
| 学習者 | 講義動画・ポッドキャストの長時間視聴 |
| 映画・動画視聴者 | セリフが小さく効果音が大きい映画の音量差解消 |
| 深夜利用者 | 家族に配慮した小音量でもセリフが聞こえる状態 |
| 聴取に不安のある利用者 | 高域の聞き取り低下などを補う日常的な補助 |

専門用語(周波数、dB、コンプレッサー等)を知らないユーザーを主対象とする。UI 上ではこれらの用語を直接露出しない。

### 1.3 スコープ外(本バージョンでは扱わない)

- マイク入力(自分の声)の加工
- 録音・ファイル書き出し機能
- macOS / Linux 対応
- 医療機器としての聴覚補助(補聴器の代替を謳わない)

---

## 2. 機能要件

### 2.1 音声処理(コア)

| ID | 要件 | 優先度 |
|---|---|---|
| F-01 | システムで再生される音声をリアルタイムに取り込み、DSP 処理後に実出力デバイスへ再生できる | 必須 |
| F-02 | 低音の濁りを抑える処理(ハイパスフィルタ+低域整理)を行える | 必須 |
| F-03 | 人の声の明瞭度を上げる処理(スピーチ帯域の強調)を行える | 必須 |
| F-04 | 音量差を自動的に整える処理(コンプレッション+ラウドネス平準化)を行える | 必須 |
| F-05 | 出力段で音割れを防ぐ(リミッター) | 必須 |
| F-06 | 処理のオン/オフを即時に切り替え、処理前後を比較試聴できる(A/B 比較) | 必須 |
| F-07 | サンプルレート 44.1k/48k Hz、2ch に対応する(その他レートはリサンプル) | 必須 |
| F-08 | 処理遅延はエンドツーエンドで 50 ms 以下を目標、**定常状態で** 100 ms を上限とする(動画リップシンク許容範囲)。音源側のストリーム切り替えに伴う一過性の増加は、リングバッファ容量の範囲内で吸収され、目標充填に復帰することをもって可とする | 必須 |

### 2.2 プリセット

| ID | 要件 | 優先度 |
|---|---|---|
| F-10 | 組み込みプリセットを提供する: 「会話」「講義」「映画」「深夜」「ゲーム」「標準(素通し)」 | 必須 |
| F-11 | プリセットはワンクリックで切り替えられ、即時に反映される | 必須 |
| F-12 | ユーザーはプリセットを元に調整した状態を独自プリセットとして名前を付けて保存できる | 必須 |
| F-13 | 独自プリセットは出力デバイス(ヘッドホン/スピーカー等)と関連付けて保存でき、デバイス切替時に自動適用できる **✅ 実装済み(2026-08-04)** | 高 |
| F-14 | プリセットのエクスポート/インポート(ファイル共有) | 低 |

組み込みプリセットの意図:

| プリセット | 意図 | 処理の傾向 |
|---|---|---|
| 会話 | 通話・会議の声を聞き取りやすく | 声帯域強調・強め、低域カット中、音量平準化・強め |
| 講義 | 長時間聴取で疲れにくく | 声帯域強調・中、音量平準化・中、高域刺激を抑制 |
| 映画 | セリフと効果音の音量差を縮める | 音量平準化・強め、声帯域強調・中、低域は残す |
| 深夜 | 小音量でも聞こえるダイナミクス圧縮 | 音量平準化・最強、低域カット強め(近隣配慮) |
| ゲーム | 足音など小さな音と、その方向を分かりやすく | 低域カット・最強、中高域強調・強め、ステレオ拡張、リミッター先読み短縮 |
| 標準 | 何もしない | 全項目・0。素通し・遅延 0 ms |

### 2.3 ユーザー調整

| ID | 要件 | 優先度 |
|---|---|---|
| F-20 | 「低音」「声の明瞭さ」「音量差」の 3 項目を、それぞれ 0〜100 のスライダーで調整できる | 必須 |
| F-21 | 各スライダーは内部の複数 DSP パラメータへマッピングされ、ユーザーに周波数・dB 値を意識させない | 必須 |
| F-22 | 全体の出力音量(処理後ゲイン)を調整できる **✅ 実装済み(2026-08-04)。**減衰のみとし、素通し時の遅延 0 ms を保つ | 必須 |
| F-23 | 「詳細モード」で EQ カーブ等を直接編集できる(上級者向け) | 低 |
| F-24 | 左右の音量バランスを調整できる **✅ 実装済み(2026-08-05)。**遠い側の減衰のみとし、素通し時の遅延 0 ms を保つ。出力デバイスごとに記憶する(F-13 と連動) | 高 |

### 2.4 UI / 操作

| ID | 要件 | 優先度 |
|---|---|---|
| F-30 | メインウィンドウ: プリセット選択、3 スライダー、A/B 比較ボタン、オン/オフを 1 画面に収める | 必須 |
| F-31 | タスクトレイ常駐。トレイメニューからオン/オフ・プリセット切替が可能 | 必須 |
| F-32 | 出力デバイスの選択 UI(既定デバイス追従も可) | 必須 |
| F-33 | 処理中であることが分かる簡易レベルメーター表示 | 高 |
| F-34 | 日本語 UI(将来的に英語対応可能な構造にする)**✅ 実装済み(2026-08-06)。**日本語・英語の 2 言語。Windows の言語に追従、または明示指定(切り替えは次回起動時)。翻訳は実行ファイルに埋め込むため、欠落して静かに英語へ戻ることがない | 必須 |
| F-35 | Windows 起動時の自動開始(オプション) | 高 |

### 2.5 将来機能(v2 以降・設計上の考慮のみ)

| ID | 要件 |
|---|---|
| F-90 | フォアグラウンドアプリに応じたプリセット自動切替(ブラウザー→会話、動画プレーヤー→映画、ゲーム→標準 等) |
| F-91 | アプリ別の音声取り込み(プロセスループバック API 併用)による、対象アプリのみの補正 |
| F-92 | 簡易な聞こえチェック(テスト音による推奨プリセット提案) |

---

## 3. 非機能要件

| ID | 分類 | 要件 |
|---|---|---|
| N-01 | 性能 | 定常時 CPU 使用率 5% 以下(4 コア相当の一般的な PC 基準) |
| N-02 | 性能 | オーディオスレッドでのメモリ確保・ブロッキング処理を行わない(グリッチ防止) |
| N-03 | 信頼性 | デバイスの抜き差し・既定デバイス変更・サンプルレート変更に追従し、無音停止しない **✅ 実装・実測済み。全経路の検証完了(2026-08-05)。**スリープ復帰は障害ではなくリングの段差として現れ、断絶が数十回から 1 回に(§4.3、聴感確認済み)。デバイス消失は間違ったデバイスへ「成功」と繋いでいたのを修正し、正しいデバイスへ 13 回目・16.4 秒で復帰することを実測(§4.4)。`docs/m3.5-robustness-notes.md` §4.3 / §4.4 / §5 |
| N-04 | 信頼性 | アプリ異常終了時、システム音声が無音のまま残らないこと(既定デバイスの復旧)**✅ 実装・実測済み(2026-08-04)。2026-08-05 に戻す先をアプリの「出力先」優先に変更**(退避したデバイスがケーブル自身だと無音のまま復旧できなかったため)。`docs/m3.5-robustness-notes.md` §4.1 |
| N-05 | 安全性 | 出力は常時リミッターを通し、想定外の大音量を出さない |
| N-06 | 互換性 | Windows 10 2004+ / Windows 11、x64(ARM64 は将来検討) |
| N-07 | 導入 | インストーラー 1 本で導入・削除できる **✅ 実装済み(2026-08-05)。**~~仮想デバイスドライバを含めて~~ の部分は対象外 — ドライバは同梱せず、利用者が VB-Cable を導入する(§4.1.1)。Inno Setup 6、管理者権限不要 |
| N-08 | 導入 | アプリとインストーラーは署名済みであること **✅ 実装済み(2026-08-06)。**~~ドライバは署名済みであること(配布形態に応じ Attestation 署名等)~~ の部分は対象外 — ドライバを配布しないため。OV 証明書で署名、タイムスタンプ付き。SmartScreen の評価については §4.1.1 |
| N-09 | 保守 | 設定・プリセットは人間可読形式(JSON)でユーザープロファイル配下に保存 |
| N-10 | プライバシー | 音声データを保存・送信しない。ネットワーク通信は行わない(将来の更新チェックを除く) |

---

## 4. 制約・前提

### 4.1 適用範囲: 一般配布する / 自作ドライバは使わない(2026-08-06 改定)

**AudioLens は一般に配布する。ただし自作のカーネルモードドライバは使用しない。**

2026-08-04 には「個人利用のみ」としていた。**その判断は 2 つの独立した理由からなり、片方だけが消えた。**

**(1) 自作カーネルドライバを使わない ― 安全性の判断。これは変わらない。**カーネルモードのコードは失敗の代償が大きい。ユーザーモードのバグはアプリが落ちるだけだが、カーネルモードのバグはブルースクリーン、起動不能、最悪の場合ファイルシステムの破損になる。仮想オーディオデバイスは AudioLens の中核ではなく音の入口にすぎず、そこに OS 全体を巻き込む故障モードを持ち込む価値はない。**VB-Cable のように広く使われ、Microsoft の署名を受けて配布されているドライバは、実地で桁違いの検証を経ている。**自作より安全である。

**(2) 一般配布しない ― 実現性の判断。これが消えた。**この理由は「ドライバを同梱するなら Partner Center 由来の Microsoft 署名が要り、その登録には EV 証明書が要り、EV は登記済みの事業体にしか出ない」というものだった。**(1) の結論としてドライバを同梱しないので、この連鎖は最初の一歩で切れている。**署名が要るのはアプリとインストーラーだけで、それは手持ちの OV 証明書でできる。

**したがって配布を妨げるものは残っていない。**当初「(1) が主で (2) は従」と書いたのは正しく、主たる理由が従たる理由を無効にした形になる。

この改定により次が変わる。

| 項目 | 変更 |
|---|---|
| 自作仮想デバイスドライバ(`driver/`)の使用 | **対象外のまま。**理由 (1) は変わらない |
| N-07(導入・削除) | **ドライバを含めない形で実装済み。**インストーラーはアプリのみを導入する |
| N-08(ドライバの署名) | **対象外のまま。**ドライバを配布しないため。アプリとインストーラーの署名は別途行う |
| F-34(英語対応) | **必須に復帰し、実装済み**(2026-08-06) |

エンジン・DSP・GUI に関する要件は一切変わらない。

### 4.1.1 配布にあたっての制約

**VB-Cable は同梱しない。利用者が自分で導入する。**

VB-Cable は VB-Audio 社の製品であり、再配布には同社との合意が要る。AudioLens のインストーラーは**導入の有無を確認して知らせるだけ**で、ドライバ本体は含めない。この設計は個人利用を前提にしていた頃から変わっておらず、配布に切り替えても手を入れる必要がない。

**利用者側のライセンスは利用者と VB-Audio の間の問題である。**VB-Cable は個人利用が無償である一方、業務・商用利用には別途ライセンスが必要とされている。AudioLens はその条件を代行できないので、**導入を案内する箇所で VB-Audio の配布元へ誘導し、条件は各自で確認してもらう。**

> 上記のライセンス条件は 2026-08 時点の理解であり、一次情報は <https://vb-audio.com/Cable/> である。配布を始める前に現行の条件を確認すること。

**SmartScreen は当面警告を出す。**手持ちの証明書は OV であり、EV と違って評価が即座には得られない。ダウンロード数が積み上がるまで「発行元不明」の警告が出る可能性が高い。これは署名の不備ではなく OV の性質である。

### 4.2 音声取り込み手段

- 音声取り込みは仮想オーディオデバイス方式を採用する(選定理由は `architecture.md` 参照)。
- **仮想デバイスには VB-Cable 等の既存ドライバを使う。同梱はしない**(§4.1.1)。M1 で「開発フェーズの割り切り」としていた構成が、そのまま最終形になる。
- 自作ドライバ(`driver/`)は開発成果の記録として残すが、ビルドも実行もしない。
- 仮想デバイスドライバの導入に管理者権限が必要となる。
- DRM 保護された一部コンテンツや排他モード(WASAPI exclusive)再生アプリは、仮想デバイス経由では音が取れない場合がある。制限事項としてユーザーに提示する。
- 開発言語は C++(C++20)。理由: WASAPI/COM とのネイティブ親和性、低遅延リアルタイム処理、ドライバ関連コードとの整合。

## 5. 成功基準(v1.0)

1. 「映画」プリセットで、セリフ音量と効果音のラウドネス差が処理前より明確に縮小する(LUFS 計測で検証)。
2. 「会話」プリセットで、音声明瞭度の主観評価が処理前より向上する(社内試聴)。
3. YouTube 再生でリップシンクの違和感がない(**定常状態の**遅延 100 ms 以下、F-08)。
4. 8 時間連続再生でグリッチ・クラッシュ・メモリリークがない。

### 5.1 基準 3 を「定常状態」で定義する理由

曲送りなど音源側がストリームを畳んで張り直す瞬間、キャプチャへのパケット供給が一度途切れ、再開時に遅れて届く。リングバッファの充填はそのぶん一時的に増え、エンドツーエンド遅延も同じだけ増える。

これは AudioLens が作る現象ではなく、通常の再生操作で必ず起きる。そして**それを吸収することがリングバッファの役目**である。外乱は音切れで払うか遅延で飲むかのどちらかで、N-01(音切れを起こさない)を最優先する以上、飲むほうを選ぶ。リングを外乱吸収に足る大きさに保ったまま瞬間値まで 100 ms に収めることは両立しない。

したがって上限は定常状態に対して課し、過渡については「容量内に収まり、目標充填へ復帰すること」を条件とする。

8 時間連続再生(2026-08-06)の実測:

| 項目 | 実測 |
|---|---|
| 定常状態の遅延 | 最小 61.0 / 平均 79.8 / 最大 95.4 ms |
| リング充填 | 平均 65.8 ms(目標 66.0 ms) |
| 過渡の頂点 | リング 95.3 ms(容量 132.0 ms、余裕 36.7 ms) |
| 音切れ | underrun 0 / overrun 0 / resync 0 |
